# Contributing

## Publication-Safe Contribution Rules

- Do not commit local machine paths, usernames, or personal identifiers.
- Never commit secrets, keys, tokens, or credentials.
- Use referenced secret managers/runtime injection instead of source-controlled secrets.
- Keep README and docs links valid and workspace-portable.
- Follow branch policy: pull requests to `main`, `dev`, `staging` with required checks green.
